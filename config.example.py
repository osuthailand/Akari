config = {
    "mysql": {
        "db": "ripples database",
        "host": "ip to mysql (usually you can just do localhost)",
        "password": "pwd",
        "user": "simon"
    },
    "akari-cikey": "any secret key here really (has to be the same as pep.py)"
}